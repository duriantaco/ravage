[project]
name = "httpx-mcp"
version = "0.1.0"
description = "MCP wrapper for scoped HTTP probing"
requires-python = ">=3.12,<3.13"
dependencies = [
    "httpx>=0.27",
    "mcp>=1.0",
    "pydantic>=2.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

